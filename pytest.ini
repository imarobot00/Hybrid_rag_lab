[pytest]
# Where the tests live.
testpaths = tests
# Only treat these files as tests.
python_files = test_*.py
python_functions = test_*
# Verbose output with short tracebacks by default.
addopts = -v --tb=short
